import registry from './templateRegistry.cjs'

const {
  canonicalizeTemplateId,
  BASE_TEMPLATE_OPTIONS,
  SUPPORTED_RESUME_TEMPLATE_IDS
} = registry

export { canonicalizeTemplateId, BASE_TEMPLATE_OPTIONS, SUPPORTED_RESUME_TEMPLATE_IDS }
