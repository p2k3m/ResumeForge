import actionableAdvice from './actionableAdviceShared.cjs'

const {
  normaliseActionList,
  formatActionList,
  inferActionType,
  buildActionableMessage,
  buildCategoryAdvice,
  buildSegmentAdvice,
  buildImprovementHintFromSegment,
  buildMetricTip
} = actionableAdvice

export {
  normaliseActionList,
  formatActionList,
  inferActionType,
  buildActionableMessage,
  buildCategoryAdvice,
  buildSegmentAdvice,
  buildImprovementHintFromSegment,
  buildMetricTip
}

export default actionableAdvice
